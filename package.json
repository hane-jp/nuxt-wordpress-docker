{
  "name": "nuxt-wordpress-docker",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix nuxt run dev",
    "build": "npm --prefix nuxt run build",
    "generate": "npm --prefix nuxt run generate",
    "test": "npm --prefix nuxt test"
  },
  "engines": {
    "node": ">=24.11.0 <25",
    "npm": ">=11.0.0"
  },
  "packageManager": "npm@11"
}
